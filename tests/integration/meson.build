# Integration tests (cross-module flows, real temp dirs, real GTK on a real
# display -- nothing here is offscreen; see AGENTS.md "Two test tracks").
# Still empty: every integration suite so far lives directly in tests/ and is
# declared in tests/meson.build, so this file exists only to keep the
# subdir() call in tests/meson.build valid.